{
  "name": "@mbongo/sdk",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "description": "TypeScript client for the Mbongo Chain JSON-RPC surface (RPC v0.2)",
  "files": [
    "dist",
    "README.md"
  ],
  "dependencies": {
    "@noble/curves": "2.4.0",
    "@noble/hashes": "2.4.0"
  }
}
