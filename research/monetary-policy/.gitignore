# Byte-code generated by running the model or its tests. This lives here,
# scoped to the research tree, rather than in the repository root gitignore:
# this is the first Python in the repo and the rule should travel with it.
__pycache__/
*.py[cod]
